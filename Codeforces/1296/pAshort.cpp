#import<iostream>

#define O std::cin
int t, d, o, e;

main() {
    for (O >> t;
         O >> t;
         puts(o % 2 || e && o ? "YES" : "NO"))
        for (o = e = 0; t--; d % 2 ? o++ : e++)O >> d;
}